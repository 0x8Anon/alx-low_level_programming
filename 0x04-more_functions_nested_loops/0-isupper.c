#include "main.h"
#include <ctype.h>
/**
 * _isupper - Entry point
 * Description: checks if c is upper
 * @c: charachter read
 * Return: 1 if upper 0 if else
 */
int _isupper(int c)
{

if (isupper(c))
	return (1);
else
	return (0);

}
