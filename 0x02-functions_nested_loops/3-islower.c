#include "main.h"
#include <ctype.h>
/**
 * _islower - Entry point
 * @c: deez
 * Description: deez
 * Return: 1 if lower 0 if not
 */
int _islower(int c)
{
if (islower(c))
{
return (1);
}
else
{
return (0);
}
}
