#include "main.h"
#include <ctype.h>
/**
 * _isalpha - deez
 * Description: deez
 * Return: 1 if a letter 0 else wise
 * @c: deez
 */
int _isalpha(int c)
{
if (isalpha(c))
{
return (1);
}
else
{
return (0);
}
}
