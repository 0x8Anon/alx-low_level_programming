#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * _abs - deez
 * Description: deez
 * Return: 0 if succesful
 * @x: deez
 */
int _abs(int x)
{
if (x > 0)
{
return (x);
}
else
{
return (x * -1);
}
_putchar('\n');
}
